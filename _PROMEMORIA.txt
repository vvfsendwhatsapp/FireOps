<!-- ==========================================================
     TURNARIO — 3 pezzi da incollare in index.html
     ==========================================================

     PEZZO 1 di 3 — CSS
     Va aggiunto in fondo al blocco <style> già presente nel <head>,
     subito prima del </style>.
     ========================================================== -->

<style>
    /* ==========================================================
       SEZIONE TURNARIO
       ========================================================== */

    .turnario-intestazione {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 12px;
        margin: 16px 0 8px;
    }

    .turnario-intestazione h4 {
        margin: 0;
        color: var(--primary-color);
        font-size: 15px;
    }

    .turnario-conteggio {
        font-family: "Courier New", monospace;
        font-size: 11px;
        color: var(--text-muted);
        border: 1px solid var(--border-color);
        border-radius: 999px;
        padding: 1px 8px;
        flex-shrink: 0;
    }

    .turnario-legenda {
        display: flex;
        flex-wrap: wrap;
        gap: 8px 18px;
        font-size: 12px;
        color: var(--text-muted);
        margin-bottom: 10px;
    }

    .turnario-legenda-voce {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .turnario-campione {
        display: inline-block;
        width: 13px;
        height: 13px;
        border-radius: 3px;
        border: 1px solid var(--border-color);
        flex-shrink: 0;
    }

    /* 31 colonne non entrano in mezzo schermo: la griglia scorre in
       orizzontale dentro il suo contenitore invece di sfondare il pannello */
    .turnario-scroll {
        overflow-x: auto;
        border: 1px solid var(--border-color);
        border-radius: 8px;
        background: var(--dark-color);
    }

    .turnario-tabella {
        border-collapse: collapse;
        font-family: "Courier New", monospace;
        font-size: 11px;
        width: max-content;
        min-width: 100%;
    }

    .turnario-tabella th,
    .turnario-tabella td {
        border: 1px solid var(--border-color);
        text-align: center;
        padding: 2px 3px;
        min-width: 44px;
    }

    .turnario-riga-mese th {
        background: var(--surface-color);
        color: var(--primary-color);
        text-align: left;
        font-size: 13px;
        letter-spacing: .5px;
        padding: 6px 10px;
        min-width: 0;
    }

    .turnario-cella-giorno {
        background: rgba(255, 255, 255, .03);
        line-height: 1.25;
    }

    .turnario-sett {
        display: block;
        font-size: 9px;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: .5px;
    }

    .turnario-num {
        display: block;
        font-weight: bold;
        color: var(--text-main);
    }

    /* Domenica in rosso tenue come su qualsiasi calendario: è un'indicazione
       di calendario civile, non un'evidenziazione di turno, quindi non deve
       competere col giallo delle giornate di servizio */
    .turnario-domenica .turnario-sett,
    .turnario-domenica .turnario-num {
        color: #d9483a;
    }

    .turnario-cella-turni {
        white-space: nowrap;
        color: var(--text-muted);
    }

    .turnario-turno {
        display: inline-block;
        padding: 1px 2px;
        border-radius: 3px;
    }

    .turnario-separatore {
        color: var(--border-color);
    }

    /* Giornata che ti riguarda: giallo pieno, la si trova scorrendo la
       pagina senza doverla leggere */
    .turnario-esatto {
        background: var(--primary-color);
        color: var(--dark-color);
        font-weight: bold;
    }

    /* Stessa squadra ma saltoturno diverso: presente ma in secondo piano */
    .turnario-parziale {
        background: rgba(255, 215, 0, .18);
        color: var(--primary-color);
    }

    /* La giornata odierna resta riconoscibile anche quando non è di servizio */
    .turnario-oggi {
        box-shadow: inset 0 0 0 2px #d9483a;
    }

    .turnario-cella-vuota {
        background: transparent;
        border-color: transparent;
    }
</style>